# F7 — Auth

**Feature** `../tasks.md#feature-7-auth` · **Design** `../tdd.md#api-contracts` · **Story** US7

## Goal

Rewrite `dime-api`'s `auth` module — the one service coupled to Fastify (`fastify.prisma`/`fastify.jwt`)
rather than a framework-agnostic `prisma` argument — into `dime-web`'s `lib/server/auth/auth.service.ts`,
injecting `prisma` directly and replacing `@fastify/jwt` with `jose`, and implement its 7
`app/api/auth/*` Route Handlers (register, login, refresh, logout, `me` GET/PATCH/DELETE, `me/password`,
google), so `dime-web` can register, log in, refresh, and manage a user's own account entirely on its own
routes. This is the last piece needed before Feature 9 can cut the frontend over to same-origin. Last
product module before F8 (CSV import/export).

## Stop-list override

Per the `ticket` skill's step 5, this ticket's subject matter — code that decides who may authenticate and
what token a caller holds — is squarely on the mandatory-pause list ("the ticket touches authentication or
authorisation"). That stop condition **fires**. The operator running this session explicitly authorized
proceeding through it without a human reading this spec first (this feature is this phase's
explicitly-flagged highest-risk piece specifically because it is a real rewrite, not a verbatim copy).
Recorded here per that instruction rather than silently proceeding. Step 3 ("Ask") is similarly overridden
for the same reason — no interactive human is available this session; every place the skill would normally
ask is instead decided below and marked accordingly.

## Decisions

| Question | Answer | Why |
|---|---|---|
| `PATCH /api/auth/me/password`'s success body — `tdd.md`'s contract row explicitly leaves this open ("no body, or `{}` — confirm at implementation via the ported service's actual return, which is `void`") | `200 {}` (empty JSON object, no message field) | **Decided autonomously — no human available this session.** `dime-api`'s own route sends a free-text `{message: 'Password updated'}`, but no other endpoint in this codebase's standardized envelope carries a bare human-readable `message` field (see `docs/api.md` — every success body is either a named-key wrapper or, for auth specifically per the contract table, a flat resource). An empty object satisfies the `void` return, needs no new field in the standardized shape, and is one of the two options the TDD's own row explicitly offered. |
| Zod v4 syntax for email validation — `tdd.md` says "Zod v4" but doesn't give the exact API surface for `.email()`, which changed between v3 and v4 | Top-level `z.email('Invalid email format')`, not the deprecated `z.string().email()` | Matches this codebase's already-established convention for the same class of built-in validator: every ported schema uses the non-deprecated top-level form for `cuid` (`z.cuid(...)`, see `categories.schema.ts`, `budgets.schema.ts`, `transactions.schema.ts`), not the deprecated `z.string().cuid()`. `common.schema.ts` is the one holdout still using the deprecated chained form; not touched here since it's out of this feature's scope. |
| Whether the 7 auth routes are subject to the global rate limiter, same as every other module | Yes, all 7, no per-route exemption | Verified against `dime-api`'s **live source** (`dime-api/src/server.ts`): `@fastify/rate-limit` is registered once, globally, before every route module including `authRoutes`, with no per-route `config: {rateLimit: ...}` override anywhere in `auth.routes.ts` (contrast `csv.routes.ts`'s explicit `{max: 10, timeWindow: '1 minute'}` override on export, which is a different, not-yet-ported module). `tdd.md`'s contract table doesn't spell this out per-endpoint for auth the way it does implicitly for every other module via the T005 dependency; reading the live system settles it rather than guessing. |
| Error-status-to-`code` mapping for the service's `httpError(statusCode, message)`-shaped throws (409 register, 401 login/refresh/google, 400 wrong-old-password, 404 unreachable-in-practice paths) | A single, small per-route mapping function translating `statusCode` → the fixed `ErrorCode` enum from `lib/server/errorResponse.ts` (`400→VALIDATION_ERROR`, `401→UNAUTHORIZED`, `404→NOT_FOUND`, `409→CONFLICT`, anything else→`INTERNAL_ERROR`) | `tdd.md`'s Architecture table already fixes this exact enum uniformly for every route handler in the phase; auth's service throws are all `httpError`-shaped (`Error & {statusCode}`), unlike categories' mixed `httpError`/`{code: 'DUPLICATE_NAME'}` throws, so one small mapper suffices instead of per-error-type branching. |
| `GET /api/auth/me` returning `404` when the token's `userId` no longer has a row — `tdd.md`'s contract row for this endpoint lists no error statuses besides the blanket `401` | Ported and kept as a real, reachable path | Not merely defensive: a user who calls `DELETE /api/auth/me` still holds a live access token for up to its 15-minute TTL. A second request with that same token (`GET`/`PATCH /api/auth/me`) legitimately hits "authenticated, but the row is gone." `dime-api`'s `getMe`/`updateMe` already handle this (`getMe` returns `null`→404; `updateMe`'s `prisma.user.update` would throw P2025 which the route maps to `500` today in `dime-api` — ported as the same `500` fallback here, since `updateMe` has no explicit not-found guard in the source to port). |

## Steps

One commit per task, subject ending in the task id. `T022` depends on `T021` (imports its service).

| Step | What | Notes |
|---|---|---|
| `T021` | Rewrite the auth service — inject `prisma`, `jose` instead of `fastify.jwt`, bcrypt/Google `tokeninfo` unchanged; port schema + tests | `lib/server/auth/auth.service.ts`, `lib/server/auth/auth.schema.ts`, `lib/server/auth/auth.service.test.ts` — independent of `T022` |
| `T022` | Implement the 7 auth route handlers + tests | `app/api/auth/{register,login,refresh,logout}/route.ts`, `app/api/auth/me/route.ts`, `app/api/auth/me/password/route.ts`, `app/api/auth/google/route.ts`, `app/api/auth/auth.routes.test.ts` — after `T021` |

## Acceptance criteria

| # | Criterion | Evidence |
|---|---|---|
| 1 | `registerUser` hashes with bcrypt, rejects a duplicate email with a `409` `httpError`, seeds default categories, and issues a token pair | `lib/server/auth/auth.service.test.ts` |
| 2 | `loginUser` rejects a wrong password and an unknown email both with a `401` `httpError`, accepts a correct one | same file |
| 3 | `issueTokens` signs an access token verifiable via `jose.jwtVerify` with the same `JWT_ACCESS_SECRET`, `HS256`, a 15-minute `exp`, and `{userId,email}` payload; persists an opaque `crypto.randomUUID()` refresh token with a 30-day `expiresAt` | same file |
| 4 | `refreshTokens` rotates — the old refresh token row is deleted and no longer valid, a new pair is issued — and rejects an invalid/expired token with `401` | same file, plus a route-level rotation test (below) |
| 5 | `logoutUser` is idempotent — deleting an already-deleted/unknown token still resolves without throwing | same file |
| 6 | `updatePassword` verifies the old password via bcrypt, rejects a wrong one with `400`, and re-hashes on success | same file |
| 7 | `googleAuth` rejects a non-200 `tokeninfo` response and a 200 response missing the `email` claim, both with `401`; creates a new passwordless user + seeds categories on first sign-in, reuses an existing user by email on repeat sign-in | same file |
| 8 | Every `/api/auth/*` route enforces the global rate limit (`429`) before touching the service, and validates its body with the ported Zod schema (`400 VALIDATION_ERROR`) | `app/api/auth/auth.routes.test.ts` |
| 9 | `register`/`login`/`google` return the live-verified flat `{accessToken,refreshToken,user}` envelope (no wrapper); `refresh` returns flat `{accessToken,refreshToken}`; `me` GET/PATCH return a flat `UserProfile`; `logout`/`DELETE me` return `204` with no body; `me/password` returns `200 {}` | same file |
| 10 | An expired access token, a token signed with the wrong secret, and a malformed/truncated JWT each `401` on `GET /api/auth/me` | same file |
| 11 | Refresh-token rotation is observable at the route level: calling `/api/auth/refresh` twice with the same (first) token succeeds once and `401`s the second time | same file |
| 12 | `POST /api/auth/logout` is idempotent at the route level — a second call with the same, now-invalidated refresh token still returns `204` | same file |
| 13 | `POST /api/auth/google` `401`s when `tokeninfo` returns a non-200 response, and when it returns 200 with a body missing the `email` claim | same file |
| 14 | Live-instance evidence: register, login, refresh (rotation), `GET/PATCH me`, `me/password`, logout, and `DELETE me` against a running `dime-web` dev server on port 3107, using a token issued by the already-running `dime-api` on port 4000 (same `JWT_ACCESS_SECRET`) to prove cross-system token verification | curl session pasted below, in this spec (no PR this time) |
| 15 | `npm run verify`'s five gates pass for every file this ticket touches | `npm run verify` output, pasted below |

## Not verified

- Kuberns' actual production `RATE_LIMIT_MAX`/window values against real traffic — this ticket exercises
  the same rate-limit path every other module already uses (`checkGlobalRateLimit`), unchanged.
- Google Sign-In's pre-existing weak spot (no `aud`/client-ID check on the `tokeninfo` response) — carried
  over unchanged per `tdd.md`'s External dependencies row; not re-verified or strengthened here.
- `updateMe`'s behavior when `prisma.user.update` throws `P2025` (row deleted between `authenticate()` and
  the update) — ported as the generic `500 INTERNAL_ERROR` fallback, matching `dime-api`'s own unguarded
  behavior; not separately reproduced against a real race, only unit-asserted via a rejected mock.

## Out of scope

- `components/settings/AccountForm.tsx`'s `FlatApiError` handling — Feature 9's `T025`, once every module
  is on the standardized envelope.
- Pointing `lib/api.ts`/`SessionProvider.tsx` at same-origin — Feature 9's `T026`.
- `e2e/*.spec.ts` retargeting — Feature 9's `T027`.
- Strengthening the Google `tokeninfo` integration (`aud` check, retry policy) — explicitly named as
  carried-over-unchanged in `tdd.md`.
- `dime-api` itself — untouched, still running as the frontend's configured backend until Feature 9.

## Review

<!-- filled in after implementation -->
